version: "3"

services:
  postgres:
    image: postgres:42.7.2
    restart: always
    environment:
      POSTGRES_DB: cloud
      POSTGRES_USER: cloud_user
      POSTGRES_PASSWORD: cloud_password
    ports:
      - 5432

